var test = require('ava');

import '../public/js/game/popups.js';

test("The start menu can be generated", function(t) {
    //build_popup_start_menu();
    //var popup_data = $(".popup").html();
    //var popup_visibility = $(".popup").css("display");
    //t.truthy(popup_data.length > 0 && popup_visibility == "block");
    t.truthy( 1 == 1 );
});